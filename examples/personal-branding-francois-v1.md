# Personal branding regression fixture

Profile: `francois`

Checks:

- unresolved profile defaults to `francois`;
- one-line header contains object, template, version and date;
- footer contains François Arnaud, role, optional organization and capacity;
- LinkedIn and skill provenance use short clickable labels;
- themes appear on one compact line;
- page number remains visible;
- branding can be disabled without changing the selected content template;
- a second profile can be added without duplicating templates;
- enumerations of three or more items use bullets unless order matters.
