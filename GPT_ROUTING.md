# GPT runtime routing policy

The canonical repository routing and promotion policy lives in [AGENTS.md](AGENTS.md).

For a `.doc` or `.docx` decision note, apply its change policy and DOCX gate together with the versioned [DOCX runtime interface](contracts/docx-runtime-interface.schema.json). This file intentionally contains no duplicate rules.
